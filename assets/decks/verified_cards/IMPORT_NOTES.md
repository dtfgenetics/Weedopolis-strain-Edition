# Asset importer notes

The approved image ZIP generated in ChatGPT is the binary source package. This repository now contains the manifest, audit, import script, and fixed digital-game paths so the PNG binaries can be committed without manual guessing.

Required binary commit paths are listed in `data/verified_card_asset_paths.json`.
